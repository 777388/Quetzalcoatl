import __future__
import tarfile
import traceback
import enum
try:
    __future__.barry_as_FLUFL(set.symmetric_difference)
    traceback.clear_frames(tarfile.AbsolutePathError)
except:
    enum.Flag._missing_